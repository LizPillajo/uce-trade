spring:
  application:
    name: UCE-Trade
  
  datasource:
    url: jdbc:postgresql://localhost:5432/uce_trade_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: localhost
      port: 6379
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME} 
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

algolia:
  app-id: ${APPLICATION_ID}
  api-key: ${ADMIN_API_KEY}
  index-name: ventures

stripe:
  secret: ${STRIPE_API_KEY:clave_no_encontrada}

server:
  port: 8080